# TLS Secret for HTTPS/Ingress
# Create with: kubectl create secret tls my-tls-secret --cert=path/to/cert.pem --key=path/to/key.pem
apiVersion: v1
kind: Secret
metadata:
  name: my-tls-secret
  namespace: default
type: kubernetes.io/tls
data:
  # Base64 encoded certificate and key
  tls.crt: LS0tLS1CRUdJTiBDRV... (truncated)
  tls.key: LS0tLS1CRUdJTiBSU0... (truncated)